// Ciclare per i primi 100 numeri e inserire in un array esclusivamente i numeri divisibili per 7 o per 8.

for (let i = 0; i <= 100; i++){
    newArraay = [(i % 7 === 0), (i % 8 === 0)]
    console.log(newArray)
}